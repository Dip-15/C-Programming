#include<stdio.h>
int main()
{
    float length,width;
    float area;
    printf("Enter a length:");
    scanf("%f",&length);
    printf("Enter a width:");
    scanf("%f",&width);
    area=length*width;
    printf("Enter a area:%f",area);
    return 0;



}
